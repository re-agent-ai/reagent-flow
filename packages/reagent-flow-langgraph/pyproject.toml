[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "reagent-flow-langgraph"
version = "0.1.0"
description = "Reagent-Flow adapter for LangGraph"
requires-python = ">=3.10"
license = "MIT"
keywords = ["ai", "agents", "testing", "llm", "langgraph"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Testing",
]
dependencies = ["reagent-flow>=0.1.0", "reagent-flow-langchain>=0.1.0"]

[project.urls]
Homepage = "https://github.com/re-agent-ai/reagent-flow"
Repository = "https://github.com/re-agent-ai/reagent-flow"
Issues = "https://github.com/re-agent-ai/reagent-flow/issues"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/reagent_flow_langgraph"]
