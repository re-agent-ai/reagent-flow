[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "reagent-ai-openai"
version = "0.1.0"
description = "Reagent-AI adapter for OpenAI Python SDK"
requires-python = ">=3.10"
license = "MIT"
dependencies = ["reagent-ai>=0.1.0", "openai>=1.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/reagent_ai_openai"]
