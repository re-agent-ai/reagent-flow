name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install tools
        run: pip install ruff

      - name: Lint
        run: ruff check packages/ examples/

      - name: Format check
        run: ruff format --check packages/ examples/

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install -e packages/ttrace-ai
          pip install -e packages/ttrace-ai-openai
          pip install -e packages/ttrace-ai-langchain
          pip install -e packages/ttrace-ai-langgraph
          pip install -e packages/ttrace-ai-crewai
          pip install mypy

      - name: Type check
        run: |
          mypy packages/ttrace-ai/src/ttrace_ai/ --strict
          mypy packages/ttrace-ai-openai/src/ttrace_ai_openai/ --strict
          mypy packages/ttrace-ai-langchain/src/ttrace_ai_langchain/ --strict
          mypy packages/ttrace-ai-langgraph/src/ttrace_ai_langgraph/ --strict
          mypy packages/ttrace-ai-crewai/src/ttrace_ai_crewai/ --strict

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install -e "packages/ttrace-ai[dev]"
          pip install -e packages/ttrace-ai-openai
          pip install -e packages/ttrace-ai-langchain
          pip install -e packages/ttrace-ai-langgraph
          pip install -e packages/ttrace-ai-crewai

      - name: Test with coverage
        run: |
          coverage run -m pytest packages/ examples/ -v
          coverage report

  build:
    runs-on: ubuntu-latest
    needs: [lint, typecheck, test]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install hatch
        run: pip install hatch

      - name: Build packages
        run: |
          (cd packages/ttrace-ai && hatch build)
          (cd packages/ttrace-ai-openai && hatch build)
          (cd packages/ttrace-ai-langchain && hatch build)
          (cd packages/ttrace-ai-langgraph && hatch build)
          (cd packages/ttrace-ai-crewai && hatch build)
